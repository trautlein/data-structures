describe('doubly linked list', function() {
  var dLL;

  beforeEach(function() {
    dLL = new DoublyLinkedList();
  });

  // it('should have methods named ..."', function() {
  //   expect(graph.forEachNode).to.be.a('function');
  // });

});
